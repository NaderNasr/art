import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  title: {
    textAlign: 'center',
    textDecoration: 'underline'
  },
  highlight: {
    color: '#BB86FC',
    fontWeight: '900'
  }
}));