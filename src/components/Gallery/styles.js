import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  mobile: {
    display: 'flex',
    alignItems: 'center'
  },
  browser: {
    display: 'flex',
    alignItems: 'center'
  }
}));